# Copy to .env next to docker-compose.yml, then: docker compose up -d --build
# Never commit the real .env.

OWNER_EMAIL=paul@wellssafety.com
OWNER_PASSWORD=change-this-to-something-long
OWNER_NAME=Paul Wells
ACCOUNT_NAME=Wells Safety LLC

# starter | pro | fleet — what the owner's account can reach.
ACCOUNT_PLAN=starter

MAX_BODY_MB=25
